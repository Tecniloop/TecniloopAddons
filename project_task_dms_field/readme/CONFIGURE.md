1. Go to **Documents > Configuration > Embedded DMS templates**.
2. Create a template for model **project.task**.
3. Select the storage and access groups.
4. If your `dms_field` setup supports it, optionally set **Assigned Users** (`user_ids`) as the user field so assignees receive explicit access.
5. Define the folder and subfolder structure that should be cloned for every task.
