This private Tecniloop module receives Parseur payloads for supplier
delivery notes and vendor bills, matches them to purchase orders,
receives stock and creates draft bills.

Receipt auto-validation is enabled per vendor
(`tl_auto_validate_receipt` on `res.partner`).
