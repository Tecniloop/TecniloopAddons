<?xml version="1.0" encoding="utf-8" ?>
<odoo>
    <template
        id="report_invoice_document_hide_without_reference"
        inherit_id="account_invoice_report_grouped_by_picking.report_invoice_document"
        priority="99"
    >
        <!-- Keep only the header row for groups with picking/order; do not render 'Without reference' -->
        <xpath expr="//t[@t-if='picking != last_picking']" position="replace">
            <t t-if="picking != last_picking">
                <t t-set="last_picking" t-value="picking" />
                <tr t-if="picking">
                    <td colspan="10">
                        <strong>
                            <span>Order:</span>
                            <span t-field="picking.sale_id.name" />
                            <t t-if="picking.sale_id.client_order_ref">
                                <span t-translation="off"> (</span>
                                <span t-field="picking.sale_id.client_order_ref" />
                                <span t-translation="off">)</span>
                            </t>
                            <span>Picking:</span>
                            <span
                                t-field="picking.date_done"
                                t-options="{'widget': 'date'}"
                            />
                            <span t-field="picking.name" />
                        </strong>
                    </td>
                </tr>
            </t>
        </xpath>
    </template>
</odoo>
