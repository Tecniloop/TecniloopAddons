<odoo>
    <data noupdate="1">
    <!-- VISTA DESCUENTOS AF -->
    <!-- SCHEDULED ACTION PARA IMPORTAR REP LOGS DESCUENTOS AF
    <record id="ir_cron_get_logs_descuentos_af" model="ir.cron">
        <field name="name">Importar RepLogs Descuentos AF</field>
        <field name="model_id" ref="tl_conn_a3erp.model_a3erp_descuentos_af"/>
        <field name="state">code</field>
        <field name="code">model.get_rep_logs_descuentos_af()</field>
        <field name="user_id" ref="base.user_root"/>
        <field name="interval_number">2</field>
        <field name="nextcall" eval="(DateTime.now() + timedelta(days=1)).strftime('%Y-%m-%d 21:15:00')" />
        <field name="interval_type">days</field>
        
        <field name="priority">2</field>
        <field name="active">True</field>
    </record>-->

    <!-- SERVER ACTION PARA IMPORTAR DESCUENTOS FAMILIA CLIENTE -->
    <record id="server_action_import_descuentos_af" model="ir.actions.server">
        <field name="name">a3ERP: Importar Descuentos AF</field>
        <field name="type">ir.actions.server</field>
        <field name="model_id" ref="tl_conn_a3erp.model_a3erp_descuentos_af" />
        <field name="state">code</field>
        <field name="code">model.getall_descuentosaf()</field>
    </record>
    </data> 
    <record id="a3erp_descuentos_af_action" model="ir.actions.act_window">
        <field name="name">Descuentos AF</field>
        <field name="res_model">a3erp.descuentos.af</field>
        <field name="view_mode">list,form</field>
        <field name="context">{'search_default_movimiento': 1, 'search_default_famcli': 1}</field>
    </record>  

    <record id="a3erp_descuentos_af_form_view" model="ir.ui.view">
        <field name="name">a3erp.descuentos.af.form.view</field>
        <field name="model">a3erp.descuentos.af</field>
        <field name="arch" type="xml">
            <form string="Descuentos AF" create="false">
                <sheet>
                    <group>
                        <group>
                            <field name="procesado" readonly="1"/>
                            <field name="error" readonly="0"/>
                            <field name="movimiento" readonly="1"/>
                            <field name="fecha" readonly="1"/>
                        </group>
                        <group>
                            <field name="codart" readonly="1"/>
                            <field name="famcli" readonly="1"/>
                            <field name="descfam" readonly="1"/>
                            <field name="desc1" readonly="1"/>
                            <field name="desc2" readonly="1"/>
                            <field name="unidades" readonly="1"/>
                            <field name="tipreg" readonly="1"/>
                        </group>
                        <group>
                            <field name="company_id"/>
                        </group>
                    </group>
                </sheet>
                <chatter/>
            </form>
        </field>
    </record>

    <record id="a3erp_descuentos_af_tree_view" model="ir.ui.view">
        <field name="name">a3erp.descuentos.af.view.tree</field>
        <field name="model">a3erp.descuentos.af</field>
        <field name="arch" type="xml">
            <list string="" create="False" 
                decoration-success="procesado == True"
                decoration-danger="error == True"
                decoration-warning="procesado == False and error == False">
                <field name="error" optional="hide"/>
                <field name="procesado" optional="hide"/>
                <field name="movimiento" optional="show"/>
                <field name="fecha" optional="show"/>
                <field name="codart" optional="show"/>
                <field name="famcli" optional="show"/>
                <field name="descfam" optional="show"/>
                <field name="desc1" optional="show"/>
                <field name="desc2" optional="show"/>
                <field name="unidades" optional="show"/>
                <field name="tipreg" optional="show"/>
            </list>
        </field>
    </record>
    
    <record id="a3erp_descuentos_af_search_view" model="ir.ui.view">
        <field name="name">a3erp.descuentos.af.search</field>
        <field name="model">a3erp.descuentos.af</field>
        <field name="arch" type="xml">
            <search>
                <filter name="procesado" string="No Procesado" domain="[('procesado','=', False)]"/>
                <group>
                    <filter string="Fam. Cliente" name="famcli" context="{'group_by': 'famcli'}"/>
                    <filter string="Movimiento" name="movimiento" context="{'group_by': 'movimiento'}"/>
                </group>                
            </search>
        </field>
    </record>  

    <!-- <menuitem name="Descuentos AF" id="a3erp_descuentos_af" parent="a3erp_base_tarifas" action="a3erp_descuentos_af_action" sequence="15"/> -->
    <!-- <menuitem name="Descuentos AF" id="import_a3erp_descuentosaf" parent="a3erp_importar"
        action="server_action_import_descuentos_af" sequence="31" /> -->
</odoo>