<?xml version="1.0" encoding="utf-8" ?>
<odoo>
    <record id="view_task_form_tier_validation" model="ir.ui.view">
        <field name="name">project.task.form.tier.validation</field>
        <field name="model">project.task</field>
        <field name="inherit_id" ref="project.view_task_form2" />
        <field name="arch" type="xml">
            <xpath expr="//header/field[@name='stage_id']" position="after">
                <field name="validation_status" invisible="1" />
            </xpath>
        </field>
    </record>

    <record id="view_task_search_form_tier_validation" model="ir.ui.view">
        <field name="name">project.task.search.tier.validation</field>
        <field name="model">project.task</field>
        <field name="inherit_id" ref="project.view_task_search_form" />
        <field name="arch" type="xml">
            <filter name="my_tasks" position="after">
                <filter
                    name="needs_review"
                    string="Needs my Review"
                    domain="[('reviewer_ids', 'in', uid), ('state', 'not in', ['1_done', '1_canceled'])]"
                    help="Tasks pending my review"
                />
                <filter
                    name="tier_validated"
                    string="Validated"
                    domain="[('validated', '=', True)]"
                    help="Tasks validated and ready to be completed"
                />
            </filter>
        </field>
    </record>
</odoo>
